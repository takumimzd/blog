---
import { format } from "@formkit/tempo"


	const {date} = Astro.props;

	const options = { year: 'numeric', month: 'long', day: 'numeric' };
	const formattedDate = format(date, "medium")
---
<time datetime={date}>{formattedDate}</time>